package  {
	// Flash stuff
	import flash.display.MovieClip;
	import flash.utils.Dictionary;
	
	public class teve2Save {

		public function teve2Save(args:Object) extends MovieClip {
			//We are saving
			
			//so we need to either confirm names and that and send to server to save as json
			//or request the server to send us the code
		}

	}
	
}
