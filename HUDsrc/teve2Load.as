package  {
	// Flash stuff
	import flash.display.MovieClip;
	import flash.utils.Dictionary;
	
	public class teve2Load {

		public function teve2Load(args:Object) extends MovieClip {
            //we are loading, time to let the user select what save to use
            //or let them enter a code in for server to validate
		}
	}
}
